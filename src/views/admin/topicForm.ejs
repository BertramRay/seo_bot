<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SEO博客 - <%= isNew ? '添加主题' : '编辑主题' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
  <style>
    body {
      font-family: 'Helvetica Neue', Arial, sans-serif;
      background-color: #f5f5f5;
    }
    .sidebar {
      background-color: #343a40;
      min-height: 100vh;
      color: white;
    }
    .sidebar a {
      color: rgba(255, 255, 255, 0.8);
      text-decoration: none;
      padding: 10px 15px;
      display: block;
    }
    .sidebar a:hover {
      color: white;
      background-color: rgba(255, 255, 255, 0.1);
    }
    .sidebar .active {
      background-color: rgba(255, 255, 255, 0.2);
      color: white;
    }
    .content {
      padding: 20px;
    }
    .tag-container {
      display: flex;
      flex-wrap: wrap;
      gap: 5px;
      margin-top: 5px;
    }
    .tag-badge {
      display: inline-flex;
      align-items: center;
      background-color: #e9ecef;
      padding: 0.25rem 0.5rem;
      border-radius: 0.25rem;
      font-size: 0.875rem;
    }
    .tag-badge i {
      margin-left: 5px;
      cursor: pointer;
      color: #6c757d;
    }
    .tag-badge i:hover {
      color: #dc3545;
    }
  </style>
</head>
<body>
  <div class="container-fluid">
    <div class="row">
      <!-- 侧边栏 -->
      <div class="col-md-2 sidebar p-0">
        <div class="py-4 px-3 mb-4">
          <h5>SEO博客管理</h5>
        </div>
        <ul class="nav flex-column">
          <li class="nav-item">
            <a href="/admin">仪表盘</a>
          </li>
          <li class="nav-item">
            <a href="/admin/topics" class="active">主题管理</a>
          </li>
          <li class="nav-item">
            <a href="/admin/posts">文章管理</a>
          </li>
          <li class="nav-item">
            <a href="/admin/generate">生成内容</a>
          </li>
          <li class="nav-item">
            <a href="/admin/settings">系统设置</a>
          </li>
        </ul>
      </div>
      
      <!-- 主内容区 -->
      <div class="col-md-10 content">
        <div class="d-flex justify-content-between align-items-center mb-4">
          <h2><%= isNew ? '添加新主题' : '编辑主题' %></h2>
          <a href="/admin/topics" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> 返回主题列表
          </a>
        </div>
        
        <!-- 主题表单 -->
        <div class="card">
          <div class="card-body">
            <form action="<%= isNew ? '/admin/topics/create' : '/admin/topics/update/' + topic._id %>" method="POST" id="topicForm">
              <div class="mb-3">
                <label for="name" class="form-label">主题名称 <span class="text-danger">*</span></label>
                <input type="text" class="form-control" id="name" name="name" value="<%= topic.name || '' %>" required>
                <div class="form-text">描述性的主题名称，将用于生成高质量内容</div>
              </div>
              
              <div class="mb-3">
                <label for="description" class="form-label">主题描述</label>
                <textarea class="form-control" id="description" name="description" rows="3"><%= topic.description || '' %></textarea>
                <div class="form-text">详细描述主题内容和方向，帮助AI生成更精准的内容</div>
              </div>
              
              <div class="row">
                <div class="col-md-6 mb-3">
                  <label for="keywordInput" class="form-label">关键词 <span class="text-danger">*</span></label>
                  <div class="input-group">
                    <input type="text" class="form-control" id="keywordInput" placeholder="输入关键词后按回车添加">
                    <button class="btn btn-outline-secondary" type="button" id="addKeywordBtn">添加</button>
                  </div>
                  <div class="tag-container" id="keywordsContainer">
                    <% if (topic.keywords && topic.keywords.length > 0) { %>
                      <% topic.keywords.forEach(keyword => { %>
                        <span class="tag-badge">
                          <%= keyword %>
                          <i class="bi bi-x" onclick="removeKeyword(this)"></i>
                          <input type="hidden" name="keywords[]" value="<%= keyword %>">
                        </span>
                      <% }) %>
                    <% } %>
                  </div>
                  <div class="form-text">添加与主题相关的关键词，将用于SEO优化</div>
                </div>
                
                <div class="col-md-6 mb-3">
                  <label for="categoryInput" class="form-label">分类 <span class="text-danger">*</span></label>
                  <div class="input-group">
                    <input type="text" class="form-control" id="categoryInput" placeholder="输入分类后按回车添加">
                    <button class="btn btn-outline-secondary" type="button" id="addCategoryBtn">添加</button>
                  </div>
                  <div class="tag-container" id="categoriesContainer">
                    <% if (topic.categories && topic.categories.length > 0) { %>
                      <% topic.categories.forEach(category => { %>
                        <span class="tag-badge">
                          <%= category %>
                          <i class="bi bi-x" onclick="removeCategory(this)"></i>
                          <input type="hidden" name="categories[]" value="<%= category %>">
                        </span>
                      <% }) %>
                    <% } %>
                  </div>
                  <div class="form-text">为主题添加分类，便于内容组织和导航</div>
                </div>
              </div>
              
              <div class="row">
                <div class="col-md-6 mb-3">
                  <label for="priority" class="form-label">优先级</label>
                  <input type="number" class="form-control" id="priority" name="priority" min="1" max="10" value="<%= topic.priority || 5 %>">
                  <div class="form-text">数值越高，生成内容时优先级越高 (1-10)</div>
                </div>
                
                <div class="col-md-6 mb-3">
                  <label for="status" class="form-label">状态</label>
                  <select class="form-select" id="status" name="status">
                    <option value="active" <%= topic.status === 'active' ? 'selected' : '' %>>活跃</option>
                    <option value="inactive" <%= topic.status === 'inactive' ? 'selected' : '' %>>停用</option>
                    <option value="draft" <%= topic.status === 'draft' ? 'selected' : '' %>>草稿</option>
                  </select>
                  <div class="form-text">只有活跃状态的主题才会参与自动内容生成</div>
                </div>
              </div>
              
              <div class="mb-3">
                <label for="promptTemplate" class="form-label">自定义提示词模板</label>
                <textarea class="form-control" id="promptTemplate" name="promptTemplate" rows="5"><%= topic.promptTemplate || '' %></textarea>
                <div class="form-text">自定义提示词模板，留空则使用默认模板</div>
              </div>
              
              <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                <a href="/admin/topics" class="btn btn-outline-secondary">取消</a>
                <button type="submit" class="btn btn-primary">保存主题</button>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // 添加关键词
    document.getElementById('addKeywordBtn').addEventListener('click', function() {
      addKeyword();
    });
    
    document.getElementById('keywordInput').addEventListener('keydown', function(e) {
      if (e.key === 'Enter') {
        e.preventDefault();
        addKeyword();
      }
    });
    
    function addKeyword() {
      const input = document.getElementById('keywordInput');
      const value = input.value.trim();
      
      if (value) {
        const container = document.getElementById('keywordsContainer');
        
        const span = document.createElement('span');
        span.className = 'tag-badge';
        span.innerHTML = `
          ${value}
          <i class="bi bi-x" onclick="removeKeyword(this)"></i>
          <input type="hidden" name="keywords[]" value="${value}">
        `;
        
        container.appendChild(span);
        input.value = '';
      }
    }
    
    function removeKeyword(element) {
      element.parentElement.remove();
    }
    
    // 添加分类
    document.getElementById('addCategoryBtn').addEventListener('click', function() {
      addCategory();
    });
    
    document.getElementById('categoryInput').addEventListener('keydown', function(e) {
      if (e.key === 'Enter') {
        e.preventDefault();
        addCategory();
      }
    });
    
    function addCategory() {
      const input = document.getElementById('categoryInput');
      const value = input.value.trim();
      
      if (value) {
        const container = document.getElementById('categoriesContainer');
        
        const span = document.createElement('span');
        span.className = 'tag-badge';
        span.innerHTML = `
          ${value}
          <i class="bi bi-x" onclick="removeCategory(this)"></i>
          <input type="hidden" name="categories[]" value="${value}">
        `;
        
        container.appendChild(span);
        input.value = '';
      }
    }
    
    function removeCategory(element) {
      element.parentElement.remove();
    }
    
    // 表单提交前验证
    document.getElementById('topicForm').addEventListener('submit', function(e) {
      const keywords = document.querySelectorAll('input[name="keywords[]"]');
      const categories = document.querySelectorAll('input[name="categories[]"]');
      
      if (keywords.length === 0) {
        e.preventDefault();
        alert('请至少添加一个关键词');
        return false;
      }
      
      if (categories.length === 0) {
        e.preventDefault();
        alert('请至少添加一个分类');
        return false;
      }
      
      return true;
    });
  </script>
</body>
</html> 